name: Build Flutter APK

on:
  push:
    branches: [ main ]
  workflow_dispatch:

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.19.6'
          channel: 'stable'

      - name: Fix pubspec.yaml fonts section
        run: |
          cd Family-Finance
          python3 << 'EOF'
          import re
          with open('pubspec.yaml', 'r') as f:
              content = f.read()
          # Remove entire fonts block
          content = re.sub(r'\s+fonts:.*?(?=\n\S|\Z)', '', content, flags=re.DOTALL)
          with open('pubspec.yaml', 'w') as f:
              f.write(content)
          print("pubspec.yaml fixed:")
          print(content)
          EOF

      - name: Create asset folders
        run: |
          cd Family-Finance
          mkdir -p assets/images assets/icons assets/fonts

      - name: Flutter pub get
        run: |
          cd Family-Finance
          flutter pub get

      - name: Generate Drift database code
        run: |
          cd Family-Finance
          dart run build_runner build --delete-conflicting-outputs

      - name: Build Debug APK
        run: |
          cd Family-Finance
          flutter build apk --debug

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: Family-Finance-APK
          path: Family-Finance/build/app/outputs/flutter-apk/app-debug.apk
          retention-days: 7
